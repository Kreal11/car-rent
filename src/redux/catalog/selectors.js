export const selectCatalog = state => state.catalog.catalog;
export const isLoading = state => state.catalog.isLoading;
export const error = state => state.catalog.error;
export const isLiked = state => state.catalog.isLiked;
export const currentId = state => state.catalog.currentId;
